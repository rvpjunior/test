name: Analyze PR

on:
  pull_request

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16'  # Ou a versão que você utiliza

      - name: Install dependencies
        run: npm install

      - name: Run tests and generate coverage report
        run: |
          npm test -- --coverage

      - name: NPM Lockfile Changes
        if: contains(github.event.pull_request.changed_files, 'package-lock.json')
        uses: codepunkt/npm-lockfile-changes@v1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Coverage Comment
        uses: ewjoachim/coverage-comment@v1
        with:
          coverage-file: coverage/coverage.xml
          github-token: ${{ secrets.GITHUB_TOKEN }}
